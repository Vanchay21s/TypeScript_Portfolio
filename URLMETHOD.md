# API Specification (v1)

### 🔐 Authentication
* **POST** `/v1/auth/signup` — `✅ Done`
* **POST** `/v1/auth/login` — `✅ Done`
* **POST** `/v1/auth/logout` — ``

--------------------------------------------------------------------

### 👤 Profile
* **POST** `/v1/user` — Create new user — `✅ Done`
* **GET** `/v1/user` — Fetch all user with Pagination & Search by name — `✅ Done`
* **GET** `/v1/user/{id}` — Fetch specific user details — `✅ Done`
* **PUT** `/v1/user/{id}` — Update user information — ``
* **PATCH** `/v1/users/{id}/role` — Change user role — ``
* **DELETE** `/v1/education/{id}` — Remove User entry — ``

--------------------------------------------------------------------

### 👤 Profile
* **POST** `/v1/profile` — Create user profile — ``
* **GET** `/v1/profile` — Fetch all profiles — ``
* **GET** `/v1/profile/{id}` — Fetch specific profile details — ``
* **PUT** `/v1/profile/{id}` — Update profile information — ``

--------------------------------------------------------------------

### 🎓 Education
* **POST** `/v1/education` — Add education entry — ``
* **GET** `/v1/education` — Fetch all education entries — ``
* **GET** `/v1/education/{id}` — Fetch specific education entry — ``
* **PUT** `/v1/education/{id}` — Update education entry — ``
* **DELETE** `/v1/education/{id}` — Remove education entry — ``

--------------------------------------------------------------------

### ⚡ Skills
* **POST** `/v1/skill` — Add new skill — ``
* **GET** `/v1/skill` — Fetch all skills — ``
* **GET** `/v1/skill/{id}` — Fetch specific skill details — ``
* **DELETE** `/v1/skill/{id}` — Remove skill record — ``

--------------------------------------------------------------------

### 💼 Work Experience
* **POST** `/v1/work` — Add employment history  — ``
* **GET** `/v1/work` — Fetch all employment history — ``
* **GET** `/v1/work/{id}` — Fetch specific employment details — ``
* **PUT** `/v1/work/{id}` — Update employment record — ``
* **DELETE** `/v1/work/{id}` — Remove employment record — ``

--------------------------------------------------------------------

### 🚀 Key Features
* **POST** `/v1/feature` — Add product feature — ``
* **GET** `/v1/feature` — Fetch all product features — ``
* **GET** `/v1/feature/{id}` — Fetch specific feature details — ``
* **DELETE** `/v1/feature/{id}` — Remove product feature — ``

--------------------------------------------------------------------

### 💻 Technology
* **POST** `/v1/technology` — Add core technology stack item — ``
* **GET** `/v1/technology` — Fetch all stack items — ``
* **GET** `/v1/technology/{id}` — Fetch specific technology details — ``
* **DELETE** `/v1/technology/{id}` — Remove technology stack item — ``

--------------------------------------------------------------------

### 🛠️ Technology Tools
* **POST** `/v1/tool` — Add development tool — ``
* **GET** `/v1/tool` — Fetch all development tools — ``
* **GET** `/v1/tool/{id}` — Fetch specific tool details — ``
* **DELETE** `/v1/tool/{id}` — Remove development tool — ``



1. ចូរនិយាយប្រាប់ខ្ញុំអំពីខ្លួនអ្នក។
2. ហេតុអ្វីអ្នកចង់ធ្វើការនៅទីនេះ?
3. តើគោលដៅនៅពេលអនាគតរបស់អ្នកគឺជាអ្វី?
4. ហេតុអ្វីយើងគួរជ្រើសរើសយកអ្នក?
5. តើភាពខ្លាំងរបស់អ្នកគឺជាអ្វី?
6. តើភាពខ្សោយរបស់អ្នកគឺជាអ្វី?
7. តើ ៥ ឆ្នាំទៀតអ្នកចង់ក្លាយទៅជាអ្វី?
8. ហេតុអ្វីអ្នកចង់ចាកចេញពីក្រុមហ៊ុនរបស់អ្នក?
9. តើអ្នកចង់បានប្រាក់ខែប៉ុន្មាន?
10. តើអ្នកអាចចាប់ផ្ដើមការងារជាមួយយើងបាននៅពេលណា?



















